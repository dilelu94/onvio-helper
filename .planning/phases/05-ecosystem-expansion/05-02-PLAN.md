---
phase: 05-ecosystem-expansion
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [~/repos/airi/package.json]
autonomous: true
requirements: [EXT-03]

must_haves:
  truths:
    - "airi dependencies are installed via pnpm"
  artifacts:
    - path: "~/repos/airi/node_modules"
      provides: "Project dependencies"
---

<objective>
Install dependencies for the airi project using pnpm.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-ecosystem-expansion/05-01-SUMMARY.md
@~/repos/airi/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install airi dependencies</name>
  <files>~/repos/airi/node_modules</files>
  <action>
    - Change directory into `~/repos/airi`.
    - Run `pnpm install` (per D-03).
    - Ensure all dependencies listed in `package.json` are installed correctly.
  </action>
  <verify>
    <automated>pnpm -C ~/repos/airi list --depth 0</automated>
  </verify>
  <done>Dependencies for airi are installed</done>
</task>

</tasks>

<success_criteria>
airi project is configured with its dependencies installed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ecosystem-expansion/05-02-SUMMARY.md`
</output>
