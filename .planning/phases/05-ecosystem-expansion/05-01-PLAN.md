---
phase: 05-ecosystem-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [MODS.md, STATE.md]
autonomous: true
requirements: [EXT-01, EXT-02, REG-01]

must_haves:
  truths:
    - "pnpm is installed and accessible via CLI"
    - "airi repository is cloned to ~/repos/airi"
    - "Installation is registered in MODS.md"
    - "Session state is updated in STATE.md"
  artifacts:
    - path: "MODS.md"
      provides: "System modification log"
    - path: "STATE.md"
      provides: "Session status update"
  key_links:
    - from: "pnpm CLI"
      to: "System shell"
      via: "Official install script"
---

<objective>
Install pnpm and clone the airi repository. Register these actions in the system logs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@MODS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pnpm</name>
  <files>MODS.md</files>
  <action>
    - Install pnpm using the official script: `curl -fsSL https://get.pnpm.io/install.sh | sh -`
    - Ensure it is added to the shell PATH.
    - Verify installation with `pnpm --version`.
    - Per D-01 and REG-01, update MODS.md with the pnpm installation details.
  </action>
  <verify>
    <automated>pnpm --version</automated>
  </verify>
  <done>pnpm is installed and registered in MODS.md</done>
</task>

<task type="auto">
  <name>Task 2: Clone airi repository</name>
  <files>STATE.md</files>
  <action>
    - Ensure `~/repos` directory exists.
    - Clone airi from `https://github.com/moeru-ai/airi` into `~/repos/airi`.
    - Per D-02 and REG-01, update STATE.md to include the addition of the airi project to the environment.
  </action>
  <verify>
    <automated>ls -d ~/repos/airi/.git</automated>
  </verify>
  <done>airi repository is cloned and registered in STATE.md</done>
</task>

</tasks>

<success_criteria>
pnpm is functional and airi repository is present on disk. Logs are updated.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ecosystem-expansion/05-01-SUMMARY.md`
</output>
