---
phase: 09-kokoro-fastapi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".planning/quick/kokoro-fastapi-install.md", ".planning/STATE.md"]
autonomous: true
requirements: [KOKORO-01, KOKORO-02, KOKORO-03]
must_haves:
  truths:
    - "Repository exists at ~/repos/Kokoro-FastAPI"
    - "espeak-ng is installed inside 'cuda-env' distrobox"
    - "uv environment is set up and dependencies are installed"
    - "Models are downloaded using the provided script"
    - "Start script 'start.sh' is configured and executable"
  artifacts:
    - path: "/var/home/dilelu/repos/Kokoro-FastAPI"
      provides: "Source code and project directory"
    - path: "/var/home/dilelu/repos/Kokoro-FastAPI/start.sh"
      provides: "Quick start script"
  key_links:
    - from: "start.sh"
      to: "distrobox enter cuda-env"
      via: "shell execution"
---

<objective>
Install and configure Kokoro-FastAPI in the local environment using the 'cuda-env' distrobox for GPU acceleration.
Purpose: Provide a fast, CUDA-accelerated TTS API locally.
Output: A fully functional Kokoro-FastAPI installation with models and a start script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/kokoro-fastapi-install.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository and System Setup</name>
  <files>/var/home/dilelu/repos/Kokoro-FastAPI</files>
  <action>
    1. Clone the repository: `git clone https://github.com/remsky/Kokoro-FastAPI /var/home/dilelu/repos/Kokoro-FastAPI`.
    2. Enter the 'cuda-env' distrobox and install `espeak-ng`: `distrobox enter cuda-env -- sudo dnf install -y espeak-ng`.
    Note: Using 'dnf' as 'cuda-env' is a Fedora 41 image (per STATE.md Decision D-09-01).
    3. Update `.planning/quick/kokoro-fastapi-install.md` to mark progress.
  </action>
  <verify>
    <automated>ls -d /var/home/dilelu/repos/Kokoro-FastAPI && distrobox enter cuda-env -- espeak-ng --version</automated>
  </verify>
  <done>Repository cloned and espeak-ng installed in the environment.</done>
</task>

<task type="auto">
  <name>Task 2: Python Environment and Dependencies</name>
  <files>/var/home/dilelu/repos/Kokoro-FastAPI/.venv</files>
  <action>
    1. Navigate to the repository: `cd /var/home/dilelu/repos/Kokoro-FastAPI`.
    2. Use `uv` to create a virtual environment and install dependencies inside 'cuda-env':
       `distrobox enter cuda-env -- bash -c "cd /var/home/dilelu/repos/Kokoro-FastAPI && uv venv && uv pip install -r requirements.txt"`
       (Check if there is a `requirements.txt` or `pyproject.toml` first).
    3. Ensure `torch` with CUDA support is installed if not already handled by dependencies.
    4. Update `.planning/quick/kokoro-fastapi-install.md` to mark progress.
  </action>
  <verify>
    <automated>distrobox enter cuda-env -- bash -c "cd /var/home/dilelu/repos/Kokoro-FastAPI && source .venv/bin/activate && python -c 'import torch; print(torch.cuda.is_available())'"</automated>
  </verify>
  <done>Python dependencies installed and CUDA availability verified.</done>
</task>

<task type="auto">
  <name>Task 3: Model Download and Quick Start Script</name>
  <files>/var/home/dilelu/repos/Kokoro-FastAPI/start.sh</files>
  <action>
    1. Download models using the project script:
       `distrobox enter cuda-env -- bash -c "cd /var/home/dilelu/repos/Kokoro-FastAPI && .venv/bin/python docker/scripts/download_model.py"`
    2. Create a `start.sh` script in the repository root:
       ```bash
       #!/bin/bash
       distrobox enter cuda-env -- bash -c "cd /var/home/dilelu/repos/Kokoro-FastAPI && source .venv/bin/activate && python main.py"
       ```
    3. Make `start.sh` executable: `chmod +x /var/home/dilelu/repos/Kokoro-FastAPI/start.sh`.
    4. Update `.planning/quick/kokoro-fastapi-install.md` and `STATE.md` to mark completion.
  </action>
  <verify>
    <automated>test -f /var/home/dilelu/repos/Kokoro-FastAPI/start.sh && test -x /var/home/dilelu/repos/Kokoro-FastAPI/start.sh</automated>
  </verify>
  <done>Models downloaded and start script created.</done>
</task>

</tasks>

<verification>
Check that the repository exists, the virtual environment is functional with CUDA, and the start script is ready.
</verification>

<success_criteria>
Kokoro-FastAPI is ready to be launched via `./start.sh` from the repository directory.
</success_criteria>

<output>
After completion, create `.planning/phases/09-kokoro-fastapi/09-01-SUMMARY.md`
</output>
