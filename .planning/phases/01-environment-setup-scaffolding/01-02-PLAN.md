---
phase: 01-environment-setup-scaffolding
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/automation/pages/BasePage.ts, src/automation/components/KendoGrid.ts, src/automation/utils/pathHelper.ts, tests/setup.test.ts]
autonomous: true
requirements: [REQ-1.2, REQ-1.3]

must_haves:
  truths:
    - "Directory structure matches the enhanced POM design"
    - "Base Page and Kendo Grid classes are implemented in TypeScript"
    - "Classes compile and pass a structural setup test"
  artifacts:
    - path: "src/automation/pages/BasePage.ts"
      provides: "Base Page Object abstraction"
      contains: "export class BasePage"
    - path: "src/automation/components/KendoGrid.ts"
      provides: "Kendo UI Grid abstraction"
      contains: "export class KendoGrid"
  key_links:
    - from: "src/automation/components/KendoGrid.ts"
      to: "src/automation/pages/BasePage.ts"
      via: "inheritance or imports"
      pattern: "import.*BasePage"
---

<objective>
Initialize the enhanced Page Object Model (POM) structure in TypeScript. This involves creating the directory scaffolding, implementing the base classes for pages and components, and verifying the architecture with a structural test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-environment-setup-scaffolding/01-RESEARCH.md
@.planning/phases/01-environment-setup-scaffolding/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Scaffold POM Directories & Path Utility</name>
  <read_first>
    - .planning/phases/01-environment-setup-scaffolding/01-RESEARCH.md
  </read_first>
  <files>src/automation/utils/pathHelper.ts</files>
  <action>
    1. Create the following directory structure if it doesn't exist:
       - src/automation/pages
       - src/automation/components
       - src/automation/fixtures
       - src/automation/utils
    
    2. Create `src/automation/utils/pathHelper.ts` to provide cross-platform path resolution:
       ```typescript
       import path from 'path';

       export const getAbsolutePath = (...paths: string[]) => {
         return path.join(process.cwd(), ...paths);
       };
       ```
    
    3. Note: The existing .js files in these directories will be replaced or supplemented by .ts versions in subsequent tasks.
  </action>
  <verify>
    <automated>ls -d src/automation/pages src/automation/components src/automation/fixtures src/automation/utils && ls src/automation/utils/pathHelper.ts</automated>
  </verify>
  <acceptance_criteria>
    - Directories pages, components, fixtures, utils exist under src/automation
    - src/automation/utils/pathHelper.ts exists
    - pathHelper.ts contains "path.join(process.cwd()"
  </acceptance_criteria>
  <done>Directory structure is ready and path utility is implemented.</done>
</task>

<task type="auto">
  <name>Implement Base POM & Kendo Grid Foundation</name>
  <read_first>
    - .planning/phases/01-environment-setup-scaffolding/01-RESEARCH.md
  </read_first>
  <files>src/automation/pages/BasePage.ts, src/automation/components/KendoGrid.ts</files>
  <action>
    Implement the core abstractions in TypeScript as recommended in 01-RESEARCH.md.
    
    1. Create `src/automation/pages/BasePage.ts`:
       - Implement class `BasePage` with `constructor(protected page: Page)`.
       - Add `waitForLoading()` method to handle Kendo loading masks (`.k-loading-mask`, `.ui-widget-overlay`).
       - Add `navigate(url: string)` method.
    
    2. Create `src/automation/components/KendoGrid.ts`:
       - Implement class `KendoGrid` with `constructor(private page: Page, private container: Locator)`.
       - Add `getRowByText(text: string)` method using `this.container.getByRole('row').filter({ hasText: text })`.
       - Add `filterByColumn(columnName: string, value: string)` method with robust selector strategies (avoiding fragile icon characters).
    
    3. Ensure both files use proper TypeScript types from `@playwright/test`.
  </action>
  <verify>
    <automated>ls src/automation/pages/BasePage.ts src/automation/components/KendoGrid.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/automation/pages/BasePage.ts exists and exports BasePage class
    - src/automation/components/KendoGrid.ts exists and exports KendoGrid class
    - BasePage.ts contains "waitForLoading"
    - KendoGrid.ts contains "getRowByText"
  </acceptance_criteria>
  <done>Base Page and Kendo Grid abstractions are implemented in TypeScript.</done>
</task>

<task type="auto">
  <name>Verify POM Architecture Setup</name>
  <read_first>
    - src/automation/pages/BasePage.ts
    - src/automation/components/KendoGrid.ts
  </read_first>
  <files>tests/setup.test.ts</files>
  <action>
    Create a structural test to ensure the new POM classes can be instantiated and used.
    
    1. Create `tests/setup.test.ts`:
       ```typescript
       import { test, expect } from '@playwright/test';
       import { BasePage } from '../src/automation/pages/BasePage';
       import { KendoGrid } from '../src/automation/components/KendoGrid';

       test('POM: base classes can be instantiated', async ({ page }) => {
         const basePage = new BasePage(page);
         expect(basePage).toBeDefined();
         
         const grid = new KendoGrid(page, page.locator('.k-grid'));
         expect(grid).toBeDefined();
       });
       ```
    
    2. Run the test: `npx playwright test tests/setup.test.ts`
  </action>
  <verify>
    <automated>npx playwright test tests/setup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/setup.test.ts exists
    - npx playwright test tests/setup.test.ts passes (exits 0)
  </acceptance_criteria>
  <done>The POM architecture is verified to be correctly structured and importable.</done>
</task>

</tasks>

<verification>
Run `npx playwright test tests/setup.test.ts` to confirm the POM structure is valid.
</verification>

<success_criteria>
- Directory structure is complete.
- `BasePage` and `KendoGrid` are implemented in TypeScript.
- `pathHelper` utility is available for cross-platform path resolution.
- Setup test passes, ensuring architecture integrity.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-setup-scaffolding/01-02-SUMMARY.md`
</output>
