---
phase: 01-configuration-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["src/services/ConfigManager.js", "tests/unit/ConfigManager.test.js"]
autonomous: true
requirements: ["CONFIG-02", "TDD-01"]
must_haves:
  truths:
    - "ConfigManager supports adding companies by Name and ID"
    - "ConfigManager supports removing companies by ID"
    - "ConfigManager returns a list of saved companies"
  artifacts:
    - path: "src/services/ConfigManager.js"
      provides: "CRUD methods for companies"
      contains: ["addCompany", "removeCompany", "getCompanies"]
    - path: "tests/unit/ConfigManager.test.js"
      provides: "TDD coverage for companies CRUD"
      contains: ["describe('ConfigManager companies')", "it('should add a company')"]
  key_links:
    - from: "src/services/ConfigManager.js"
      to: "this.config.companies"
      via: "array manipulation"
      pattern: "this.config.companies = \\[\\]"
---

<objective>
Implement the core logic for company list management (CRUD) in ConfigManager.js following TDD.

Purpose: To allow the user to manage multiple companies (Name and ID) for which automation scripts will run.
Output: Updated ConfigManager.js with getCompanies, addCompany, and removeCompany methods, and updated tests.
</objective>

<execution_context>
@/home/dilelu/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/ConfigManager.js
@tests/unit/ConfigManager.test.js

<interfaces>
ConfigManager.js:
```javascript
class ConfigManager {
  setUser(user);
  getUser();
  save(filePath, config);
  load(filePath);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add companies CRUD tests (RED)</name>
  <files>tests/unit/ConfigManager.test.js</files>
  <action>
    Add a new describe block for 'Companies CRUD' in tests/unit/ConfigManager.test.js.
    Include tests for:
    - getCompanies(): returns an empty array by default.
    - addCompany({ name, id }): adds a company to the list.
    - removeCompany(id): removes a company by its ID.
    - getCompanies(): returns the updated list of companies.
    
    Ensure the tests fail (RED) because the methods don't exist yet.
  </action>
  <verify>npm test tests/unit/ConfigManager.test.js (Should fail with "not a function" errors)</verify>
  <done>Failing tests for companies CRUD are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement companies CRUD (GREEN)</name>
  <files>src/services/ConfigManager.js</files>
  <action>
    Update ConfigManager class:
    - Initialize `this.config.companies = []` in the constructor.
    - Implement `getCompanies()`: returns `this.config.companies`.
    - Implement `addCompany(company)`: pushes `{ name, id }` to the companies array.
    - Implement `removeCompany(id)`: filters out the company with the matching ID.
    
    Ensure all tests in tests/unit/ConfigManager.test.js pass.
  </action>
  <verify>npm test tests/unit/ConfigManager.test.js (All tests should pass)</verify>
  <done>CRUD logic implemented and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update save/load for companies</name>
  <files>src/services/ConfigManager.js, tests/unit/ConfigManager.test.js</files>
  <action>
    Update the `save` and `load` methods (if needed) to ensure that the `companies` list is correctly persisted to and from JSON.
    Add a test case in tests/unit/ConfigManager.test.js that verifies:
    - Saving a config with companies.
    - Loading it back and checking that companies are intact.
  </action>
  <verify>npm test tests/unit/ConfigManager.test.js</verify>
  <done>Persistence logic updated to include companies.</done>
</task>

</tasks>

<success_criteria>
- ConfigManager handles a list of companies (Add/Remove/Get).
- All tests for ConfigManager pass (existing + new CRUD tests).
- Persistence tests confirm companies are saved and loaded correctly.
</success_criteria>

<output>
After completion, create .planning/phases/01-configuration-management/01-01-SUMMARY.md
</output>
