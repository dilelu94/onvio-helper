---
phase: 10-openai-codex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json"]
autonomous: true
requirements: [OPENAI-01]

must_haves:
  truths:
    - "The 'openai' package is present in package.json dependencies."
    - "A script can import and initialize the OpenAI client."
  artifacts:
    - path: "package.json"
      provides: "OpenAI dependency"
    - path: "scripts/verify-openai.js"
      provides: "Verification of installation"
  key_links:
    - from: "scripts/verify-openai.js"
      to: "openai"
      via: "import"
      pattern: "import OpenAI from 'openai'"
---

<objective>
Install the OpenAI library (Codex) as a dependency in the 'onvio-helper' project and verify the installation.

Purpose: Enable AI-powered features using the OpenAI/Codex API.
Output: 'openai' package installed and verified.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install openai package</name>
  <files>package.json</files>
  <action>
    Install the 'openai' package using npm. Ensure it is added to the 'dependencies' section of package.json.
    Run: `npm install openai`
  </action>
  <verify>
    <automated>grep -q "\"openai\":" package.json</automated>
  </verify>
  <done>'openai' is listed in dependencies and node_modules contains the package.</done>
</task>

<task type="auto">
  <name>Task 2: Create verification script</name>
  <files>scripts/verify-openai.js</files>
  <action>
    Create a simple Node.js script to verify the 'openai' package can be imported and initialized.
    The script should:
    1. Import OpenAI from 'openai'.
    2. Try to instantiate the OpenAI client (even without a key, it should fail on auth, not on import).
    3. Log success if the class is correctly initialized.
  </action>
  <verify>
    <automated>node scripts/verify-openai.js</automated>
  </verify>
  <done>Verification script runs and confirms the library is available.</done>
</task>

</tasks>

<verification>
1. Run `npm list openai` to check installation.
2. Run `node scripts/verify-openai.js` to confirm import and basic usage.
</verification>

<success_criteria>
- package.json includes 'openai'.
- node_modules contains 'openai'.
- Verification script passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-openai-codex/10-01-SUMMARY.md`
</output>
