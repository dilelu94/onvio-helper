---
phase: 07-airi-connection-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Bazzite-dotfile/aliases.sh, ~/.bashrc, repos/airi/, .planning/STATE.md, .planning/ROADMAP.md]
autonomous: true
requirements: [AIRI-01]

must_haves:
  truths:
    - "airi alias in Bazzite-dotfile/aliases.sh uses DISCORD_TOKEN"
    - "airi alias in ~/.bashrc uses DISCORD_TOKEN"
    - "repos/airi/ directory is removed"
  artifacts:
    - path: "Bazzite-dotfile/aliases.sh"
      provides: "Discord token fix for aliases repo"
    - path: "/var/home/dilelu/.bashrc"
      provides: "Discord token fix for current session"
    - path: ".planning/STATE.md"
      provides: "Updated session state"
---

<objective>
Correct the Discord token environment variable name in the 'airi' alias and clean up the environment by removing the redundant airi repository.

Purpose: Fix connection issues with the Airi bot and ensure the system uses the Flatpak version.
Output: Updated aliases, removed repository, and updated documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Bazzite-dotfile/aliases.sh
@~/.bashrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Discord token variable name</name>
  <files>Bazzite-dotfile/aliases.sh, ~/.bashrc</files>
  <action>
    - Change 'DISCORD_BOT_TOKEN' to 'DISCORD_TOKEN' in the 'airi' alias within both files.
    - Ensure the rest of the alias (token value, Flatpak command) remains unchanged.
  </action>
  <verify>
    <automated>grep "DISCORD_TOKEN=" Bazzite-dotfile/aliases.sh ~/.bashrc && ! grep "DISCORD_BOT_TOKEN=" Bazzite-dotfile/aliases.sh ~/.bashrc</automated>
  </verify>
  <done>Aliases updated to use the correct environment variable name.</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup and Documentation</name>
  <files>repos/airi/, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    - Remove the 'repos/airi/' directory recursively.
    - Add a record of this change in '.planning/STATE.md' under 'Quick Tasks Completed'.
    - Update '.planning/ROADMAP.md' to include Phase 07 and mark this plan as complete (since it's a quick phase).
  </action>
  <verify>
    <automated>ls -d repos/airi/ 2>/dev/null || echo "Removed"; grep -i "Airi Connection Fix" .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <done>Redundant repository removed and changes documented in planning files.</done>
</task>

</tasks>

<verification>
Check that 'airi' alias works in a new shell or by sourcing the modified files.
</verification>

<success_criteria>
The 'airi' alias uses the correct variable name and the repository is gone.
</success_criteria>

<output>
After completion, create `.planning/phases/07-airi-connection-fix/07-01-SUMMARY.md`
</output>
